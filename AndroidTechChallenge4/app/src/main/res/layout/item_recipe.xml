<?xml version="1.0" encoding="utf-8"?>
<TableRow xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recipe_table_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:weightSum="7">

    <LinearLayout
        android:background="?attr/selectableItemBackground"
        android:padding="@dimen/item_selection_padding"
        >

        <TextView
            android:id="@+id/recipe_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:text="@string/selection_recipe_name"
            android:textAlignment="viewStart"
            android:textSize="@dimen/selection_text_size" />

        <TextView
            android:id="@+id/recipe_cooktime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="@string/selection_recipe_cooktime"
            android:textAlignment="viewEnd"
            android:textSize="16sp" />

    </LinearLayout>

</TableRow>